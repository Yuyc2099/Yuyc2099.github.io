# 博客文章编写约束

## 代码展示

- 分析函数、中断处理函数或调度流程时，按照“简化代码 → 原始源码”的顺序展示。
- 简化代码必须包含对应的函数签名，不要只放脱离函数上下文的语句。
- 使用 `// ...` 表示省略的初始化、调试、错误处理或其他无关代码。
- 简化代码必须保留与正文结论直接相关的条件、宏分支和调用顺序。
- 汇编函数可以使用同名的 C 风格伪代码表示逻辑，但正文必须明确说明它是伪代码。
- 全局变量或全局数组不属于任何函数，应明确标注为全局定义，不要添加虚假的函数上下文。
- 单个表达式或位运算可以使用行内代码；不要为一条孤立语句单独创建代码块。

简化代码示例：

```c
void rt_schedule(void)
{
    // ...
    if (scheduler_is_unlocked())
    {
        to_thread = get_highest_ready_thread();
        // ...
    }
    // ...
}
```

## 调用链伪代码

- 调用关系、调度流程和中断流程统一使用 `calltree` 代码块；不要使用 `c` 冒充可编译代码。
- 伪代码必须以真实入口函数或真实触发事件为根节点，只保留与主题直接相关的调用、判断、关键变量和状态变化。
- 函数名、变量名、判断顺序、提前返回和公共出口必须贴合对应版本的真实源码；允许省略寄存器搬运等细节，但不能改变控制流含义。
- 不要把概念步骤伪装成真实函数。概念节点直接写中文说明，不添加 `()`；真实函数才使用 `function()` 形式。
- 每个函数或步骤的作用使用 `//` 写在对应伪代码的上一行，不使用行尾注释，也不要放到代码的下一行。
- 长行优先精简参数或将说明移到上一行，不要通过破坏树形层级的方式强行换行。
- 上层流程每次只展开一层。后文会单独分析的函数在当前图中作为终点，并链接到对应章节或文章；详细逻辑只在目标位置展示一次。
- 同一流程存在不同路径时，应明确保留 `if`、`else`、提前 `return` 和公共出口，不能为了缩短流程而合并成错误的执行顺序。
- 调试、Hook、日志和与主题无关的条件编译可以省略；如果条件编译会改变正文结论，则必须保留。
- 调用树作为函数逻辑的简化表示时，后面仍按“调用树 → 原始源码”的顺序附上完整源码。

内部跳转使用稳定的英文语义锚点，不链接自动生成的中文标题 ID，也不要在锚点中包含章节编号：

```html
<div id="rt-hw-context-switch" class="section-anchor"></div>
```

`calltree` 中使用 Markdown 链接语法。链接文字可以继续参与函数、变量和阶段标记高亮：

```calltree
rt_schedule()
│   // 线程环境提交切换请求，具体逻辑见后文
└── [rt_hw_context_switch(&from_thread->sp, &to_thread->sp)](#rt-hw-context-switch)
```

跨文章链接使用文章路由，并在当前图中停止展开目标文章负责的逻辑：

```calltree
rt_schedule()
│   // 最高就绪优先级的计算由专门文章说明
└── [获取 highest_ready_priority](/articles/rt-thread-ready-priority-bitmap/)
```

## 原始源码附件

- 简化代码之后应附上对应版本的原始完整函数源码。
- 原始源码保存到文章目录的 `snippets/` 中：

```text
content/posts/<slug>/snippets/<function-name>.c
content/posts/<slug>/snippets/<function-name>.S
```

- 原始源码应从文章所分析的版本直接复制，保留函数签名、条件编译和关键注释，不使用 `// ...` 省略。
- 不要为了符合正文风格而改写原始源码。
- 使用 `{% source %}` 指令将源码附在简化代码之后：

```text
{% source file="snippets/rt_schedule.c" lang="c" title="rt_schedule()" origin="src/scheduler.c" %}
```

- `file` 指向文章目录内的源码文件。
- `lang` 填写代码语言，例如 `c`、`asm`。
- `title` 使用真实函数名或中断处理函数名。
- `origin` 填写源码在原项目中的相对路径。
- 同一个完整函数在一篇文章中只引用一次，避免重复展示。

## 验证

- 修改文章或源码附件后，运行 `npm run build`。
- 确认源码文件存在、`{% source %}` 路径有效，并且博客构建通过。
