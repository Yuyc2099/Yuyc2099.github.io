---
title: "嵌入式软件缺陷深入分析：内存、栈与 RTOS 同步"
slug: embedded-software-fault-analysis
date: 2026-06-25
updated: 2026-07-22
summary: "深入分析嵌入式系统中的堆破坏、线程栈溢出、死锁、优先级反转、IPC 误用与并发竞态，并整理 RTOS 现场保存方法。"
category: debugging
tags: [嵌入式, RTOS, RT-Thread, 内存, 栈溢出, 死锁, IPC]
series: debugging-notes
cover: ./images/cover.svg
coverAlt: "嵌入式软件中的内存、线程栈与 RTOS 同步关系示意图"
readingTime: 25
draft: false
---

上一篇 [Cortex-M 异常分析](/articles/cortex-m-fault-analysis/) 介绍了异常栈帧、HardFault 定位和常见软件缺陷。本篇继续深入软件侧：不仅关注最终进入了哪一种 fault，还要追查是哪一次越界写、错误释放或同步操作破坏了系统状态。

文中的同步原理适用于常见 RTOS，接口和部分行为以 RT-Thread 为例。不同 RT-Thread 版本以及其他 RTOS 的配置项、递归互斥量和 Hook 接口可能不同，使用时应以目标工程的头文件和内核源码为准。

---

## 1. 软件缺陷为什么难以定位

软件缺陷不一定在发生时立即触发 CPU fault。例如数组越界可能先覆盖相邻对象，数秒后才因函数指针或返回地址损坏而崩溃；死锁和优先级反转则通常不会进入 HardFault，而是表现为任务长期阻塞或系统无响应。

```text
首次错误操作
    -> 内存、栈或同步状态被破坏
    -> 系统继续运行
    -> 被破坏的数据再次被使用
    -> CPU fault、任务阻塞或看门狗复位
```

因此要区分两个位置：

- **异常暴露点**：栈帧 PC、断言或看门狗记录的位置；
- **缺陷发生点**：第一次越界写、错误释放或错误加锁的位置。

异常现场能帮助确定暴露点，但定位此类问题的目标通常是继续向前找到缺陷发生点。

---

## 2. 堆内存异常

### 2.1 常见类型

| 类型 | 说明 | 常见后果 |
|------|------|----------|
| 越界写 | 写到分配块的前后边界之外 | 覆盖相邻对象或堆管理信息 |
| 释放后使用 | 内存已归还，但旧指针继续访问 | 数据随机变化、再次分配后互相覆盖 |
| 重复释放 | 同一内存块被释放两次 | 可能触发分配器断言或被拒绝；未检测时可能破坏空闲结构 |
| 非法释放 | 释放非堆地址或分配块内部地址 | 可能触发分配器断言或被拒绝；未检测时可能破坏堆管理信息 |
| 内存泄漏 | 分配后失去引用，无法释放 | 可用堆逐渐减少，最终分配失败 |

重复释放和非法释放能否在 `free` 当场被发现，取决于分配器实现、完整性检查和断言配置，不能假设所有分配器都会以相同方式处理。

堆管理器通常会在用户数据附近保存块大小、空闲状态或链表指针。一处很小的越界写也可能先破坏这些元数据，直到后续执行 `malloc`/`free` 时才暴露。

### 2.2 哨兵值

哨兵值是在用户区前后放置固定标记，分配、释放或巡检时检查标记是否被改写。

```text
[分配信息][前哨兵][用户数据区][后哨兵]
```

这个方法通常需要包装 `malloc`/`free`、替换项目的分配接口，或者直接扩展内存管理器。实现时还应保证返回给用户的地址继续满足对齐要求。

哨兵只能证明“边界曾被覆盖”，不能直接指出是哪条指令写坏了内存。若只在释放时检查，发现时间可能已经远离首次越界；若堆元数据已经严重损坏，也不应继续盲目遍历整个堆。

### 2.3 分配与释放 Hook

RTOS 或 C 运行库提供的 malloc/free Hook 可以记录：

- 分配和释放地址；
- 请求大小；
- 当前线程；
- 返回地址或调用位置；
- 时间戳和剩余堆空间。

Hook 本身只是事件入口，不会自动检测越界、重复释放或泄漏。要完成检测，还需要维护活动分配表、哨兵或统计信息。记录逻辑应避免再次动态分配、阻塞或递归调用被 Hook 的接口，否则诊断代码本身可能引入死锁和重入问题。

RT-Thread 的 Hook 能力受版本和配置影响，应先确认目标工程是否启用了相关 Hook，并以当前版本提供的接口为准。

### 2.4 检测方式对比

| 方法 | 原理 | 适用范围与限制 |
|------|------|----------------|
| 哨兵值 | 在分配块边界写入标记并校验 | 轻量；发现时间取决于检查时机 |
| malloc/free Hook | 记录每次分配和释放 | 便于追踪生命周期；检测能力取决于额外记录逻辑 |
| 定时全量遍历 | 周期性检查活动块和哨兵 | 覆盖面广；发现滞后，且要求堆结构仍可安全遍历 |
| 可疑节点插桩 | 在怀疑的操作前后主动校验 | 定位直接；需要先缩小怀疑范围 |
| 数据观察点 | 监控指定地址的首次写入 | 能捕获写入指令；依赖芯片调试硬件和调试器，数量有限 |

> 数据观察点不是所有 Cortex-M 都必然具备。Cortex-M0 的硬件调试为可选配置，最多可实现两个观察点；具体 MCU 如果没有实现相应调试单元，该方法就不可用，使用前应查阅芯片手册和调试器能力。

### 2.5 堆内存问题排查方法

如果分配器在 `free` 中断言，只能说明异常在此时被发现，不能说明当前 `free` 就是破坏堆的操作。为了把发现时间尽量靠近破坏时间，可以在调试版本中按下面几层增加检测。

#### 哨兵与分配信息记录

首先在每个用户区前后设置哨兵，并为活动内存块记录：

- 用户区地址、申请大小和前后哨兵地址；
- 申请线程、申请序号、申请调用位置和申请时间。

诊断记录应保存在独立的固定内存池中，最近的分配和释放轨迹则可保存在静态环形缓冲区中。轨迹中的每条事件应包含操作类型、地址、大小、线程、调用位置和时间。两者都不能再次使用被诊断的堆，否则堆越界可能连同诊断信息一起破坏。

发现哨兵损坏后还要区分破坏方向：前哨兵损坏可能来自当前块向低地址越界，也可能来自前一相邻块向高地址越界；后哨兵损坏则重点检查当前块向高地址越界和后一相邻块向低地址越界。记录相邻块关系有助于继续缩小范围。

哨兵只能发现跨越分配边界的访问。如果写入仍落在整个用户区内部，只是越过了其中某个数组或成员的逻辑边界，块级哨兵不会变化，需要继续依靠代码插桩或硬件数据观察点定位。

#### 定时校验

定时器只负责唤醒低优先级检测线程，由检测线程在取得与分配器一致的保护后，遍历活动内存块并检查：

- 前后哨兵是否完整；
- 块地址、大小、对齐和范围是否合法；
- 相邻物理块是否重叠；
- 空闲链表节点是否位于堆区，前后指针是否一致；
- 活动记录与分配器中的占用状态是否一致。

不应直接在中断或软件定时器回调中执行全量遍历。检测周期越短，越容易缩小破坏发生的时间窗口，但占用的 CPU 时间也越多，可先使用较长周期，问题复现后再逐步缩短。

遍历前应确认分配器没有处于更新状态，遍历过程中还要限制最大节点数并校验下一个节点的地址。发现第一个异常后立即停止遍历，避免损坏的链表导致非法访问或死循环。

#### 毒化与延迟复用

释放内存时用固定值覆盖用户区，并将该块暂时放入隔离队列，不立即交回可分配链表。隔离期间内容再次变化，通常说明仍有旧指针在写入；重新分配前再检查一次毒化值，可以发现释放后使用。

隔离队列可按时间或容量淘汰最早释放的块。该方法会增加内存占用并改变分配时序，只适合调试版本。

#### MPU 保护与硬件数据观察点

MPU 和硬件数据观察点解决的问题不同：

- **MPU 保护**：在可疑内存边界设置禁止访问区域，使 CPU 越界访问立即进入 fault。它适合保护能够单独划分区域的缓冲区，但受区域数量、大小和对齐限制，不适合为每一个小堆块都设置保护区。Cortex-M0 没有 MPU，Cortex-M0+ 的 MPU 为可选配置；其他内核也应确认目标芯片是否实现并启用 MPU。
- **硬件数据观察点**：已经确定被改写的地址时，对该地址设置写观察点，重新运行后可在写入指令处暂停。它依赖芯片调试硬件和调试器，数量也很有限；Cortex-M0 的观察点为可选实现，目标芯片未实现时不可用。

这两种机制主要监控 CPU 访问。若内存也会被 DMA 修改，还需要单独检查 DMA 的源地址、目的地址和传输长度。

#### 每线程活动内存链表与调度校验

当周期校验的时间窗口仍然过大时，可以按申请线程组织活动内存块，并在线程切出时检查该线程持有内存块的哨兵：

```text
线程 A 活动内存链表
    -> 内存块 A1
    -> 内存块 A2
    -> 内存块 A3

线程 A 即将切出
    -> 校验 A1、A2、A3 的前后哨兵
    -> 哨兵正常：继续切换
    -> 哨兵损坏：保存现场并停止运行
```

如果前一个内存块连续向高地址越界并踩到当前内存块，前一个块的后哨兵通常会先被破坏。在线程切出时进行校验，可以把发现范围缩小到该次线程运行区间，并得到相关内存块的申请线程和申请位置。

调度 Hook 中只能进行有上限的同步校验，不能动态分配、打印日志、阻塞或调用复杂 RTOS API。遍历时必须验证记录节点和内存块地址，并限制最大节点数量。

释放内存时，应先检查哨兵，再根据记录中的原始申请线程从对应链表移除节点，最后调用真实的释放接口。不能假设释放线程就是申请线程，因为内存可能跨线程传递。`realloc` 应更新原记录；线程退出前应检查未释放块；系统启动阶段无所属线程的申请可放入独立的系统链表。

活动链表更新和哨兵校验必须与内存申请、释放保持一致。可以使用与分配器相同的临界区，或者在 `heap_updating` 有效时跳过调度校验，并在申请或释放完成后补做一次检查。

申请线程不一定是实际写坏内存的线程。共享缓冲区、中断和 DMA 都可能修改其他线程申请的内存，因此检测结果只能说明“在线程切出时首次发现其持有块损坏”，不能仅凭内存归属认定责任线程。

#### 首次异常的现场冻结

首次发现哨兵或堆结构异常时，应立即冻结静态记录，并保存损坏地址、当前线程、被切出线程、PC/LR、最近的内存操作轨迹和构建标识。此后不要继续分配内存或遍历已经不可信的堆，也不要在异常路径中打印依赖动态内存的大量日志。

最终崩溃的 `free` 通常只是暴露点。分析时应回看同一地址的分配与释放轨迹，并结合最后一次校验正常和第一次校验失败的时间范围，寻找真正的越界写或释放后使用。

---

## 3. 线程栈异常

### 3.1 栈溢出的来源

RTOS 通常为每个线程分配独立栈。以下情况会使实际使用量超过预留空间：

- 调用层次过深或出现递归；
- 局部数组、结构体或格式化缓冲区过大；
- 变长数组或 `alloca` 的运行期申请量超过预期；
- 中断嵌套增加 MSP 使用量；
- 浮点上下文、库函数或编译选项改变单层调用的栈开销。

在常见 Cortex-M RTOS 移植中，线程使用 PSP，异常处理使用 MSP，但这不是仅凭 RTOS 名称就能确定的规则，应以具体移植层为准。线程栈和异常栈需要分别评估。

### 3.2 检测手段

| 方法 | 原理 | 适用范围与限制 |
|------|------|----------------|
| 栈边界魔术字 | 在线程栈边界放置标记并定期检查 | 实现简单；检查前可能已经覆盖相邻数据 |
| 栈水位填充 | 初始化时填充整段栈，统计未被覆盖区域 | 能保留历史最大使用深度；不能替代边界保护 |
| MPU 保护区域 | 在栈增长方向的边界设置禁止访问区域 | 越界时立即产生 fault；依赖 MPU 和正确的任务切换配置 |
| 编译期分析 | 用 GCC `-fstack-usage` 等生成逐函数栈占用 | 可提前发现大栈帧；仍需结合调用图和运行路径 |

栈边界魔术字应放在栈增长方向上的可用空间边界，用于判断栈是否已经越过预设范围。具体位置应根据处理器和 RTOS 移植中的栈增长方向确定。栈水位只要被历史峰值写过，填充值通常就会留下痕迹；其局限是只能反映已经运行过的路径，而且真正越界后系统状态可能已经损坏。

### 3.3 MPU 保护的异常表现

MPU 使用的是保护区域，不是 MMU 意义上的内存页。对于向低地址增长的栈，通常在栈下边界设置不可访问区域：

```text
低地址
[MPU 禁止访问区域]
[线程栈可用区域    ]  <- SP 向低地址增长
高地址
```

- Cortex-M3/M4：MPU 访问违规通常触发 MemManage Fault；若该 fault 未使能或无法处理，则可能升级为 HardFault；
- Cortex-M0：没有 MPU；
- Cortex-M0+：可选 MPU，但没有独立 MemManage Fault，违规通常进入 HardFault。

如果 MPU 区域数量不足以同时保护全部线程，RTOS 可以在上下文切换时更新当前线程的保护区域。更新过程必须与任务切换保持一致，否则可能把正常访问误判为越界。

### 3.4 编译期栈分析的边界

GCC `-fstack-usage` 生成的 `.su` 文件描述的是逐函数栈帧大小，不会自动给出某个线程入口到最深调用点的总栈需求。还需要结合调用图、中断嵌套和库函数进行累加。

动态栈分配和递归会降低静态结果的确定性；函数指针和间接调用也可能使调用图不完整。因此编译期分析适合提前筛查，运行期仍应保留栈水位或边界检查。

> 推荐组合：开发阶段使用编译期分析和栈水位统计，高可靠场景再增加 MPU 边界保护。三者解决的问题不同，不能相互替代。

---

## 4. 死锁

死锁是多个执行单元互相等待资源，导致相关线程永久无法继续运行。它通常不会触发 Cortex-M fault，因此仅依赖 HardFault 日志无法发现。

### 4.1 四个必要条件

以下条件同时成立时才可能形成死锁：

| 条件 | 描述 |
|------|------|
| 互斥 | 资源同一时刻只能由一个线程持有 |
| 请求并保持 | 线程持有已有资源时继续申请其他资源 |
| 不可剥夺 | 资源只能由持有者主动释放 |
| 环路等待 | 多个线程形成循环等待链 |

固定全局加锁顺序可以破坏环路等待。使用超时只能让等待者获得退出机会；超时路径还必须释放已持有资源并回滚状态，否则不能自动消除死锁条件。

### 4.2 常见形式

| 类型 | 场景 | 处理方向 |
|------|------|----------|
| 同线程重复获取 | 同一线程再次获取不支持递归的锁或信号量 | 明确对象是否支持递归，检查调用链 |
| ABBA 死锁 | 线程 A 持有锁 1 等锁 2，线程 B 持有锁 2 等锁 1 | 统一加锁顺序 |
| 异常路径未解锁 | 函数提前返回或错误分支漏掉释放 | 集中清理出口，检查所有返回路径 |
| 持锁等待事件 | 持有互斥量时等待另一个线程完成，而对方也需要该锁 | 等待前释放锁，重新划分临界区 |

RT-Thread 不同版本对递归互斥量的支持可能不同，应以目标版本的接口说明和 `rt_mutex_take()` 实现为准：如果当前持有线程再次获取时只增加递归持有计数，就不会因此阻塞；如果不支持递归，则可能阻塞或返回错误。普通信号量没有所有权，计数耗尽后同一线程再次获取也会进入等待。

### 4.3 现场分析

分析死锁需要保存线程和 IPC 状态，而不只是 CPU 寄存器：

- 每个线程的运行、就绪和阻塞状态；
- 线程正在等待的对象及超时时间；
- 互斥量当前持有者和递归持有计数；
- 各 IPC 对象的等待队列；
- 最近的加锁、解锁和超时记录。

将“线程 -> 等待对象 -> 对象持有者”连成等待关系图，若形成闭环，就能直接看出死锁链。

---

## 5. 优先级反转

优先级反转与死锁不是同一个问题。典型场景如下：

```text
低优先级线程 L 持有锁
    -> 高优先级线程 H 请求该锁并阻塞
    -> 中优先级线程 M 持续抢占 L
    -> L 无法及时运行并释放锁，H 的等待时间被拉长
```

优先级继承会临时提升锁持有者 L 的优先级，使其尽快完成临界区并释放锁。RT-Thread 的互斥量提供优先级继承，而普通信号量不提供，因此保护共享资源时不应因为二者都能表示 0/1 就混用。

优先级继承只能缓解这种阻塞，不能修复环路等待、无限长临界区或持锁执行阻塞 I/O。设计时仍应缩短持锁时间，避免在持锁期间等待其他事件，并明确嵌套锁的顺序。

---

## 6. 信号量、互斥量与其他 IPC

### 6.1 信号量与互斥量

| 维度 | 信号量（Semaphore） | 互斥量（Mutex） |
|------|---------------------|-----------------|
| 所有权 | 通常没有所有权，可由其他上下文释放 | 由持有线程拥有，只能由持有者释放 |
| 数值/状态 | 可以是二值或计数值 | 对外表现为锁定/未锁定，递归实现还会维护持有计数 |
| 优先级继承 | 通常不提供 | RT-Thread 互斥量提供；其他 RTOS 需查对应文档 |
| 递归获取 | 不具备递归语义 | 取决于 RTOS 和具体版本，应查接口说明与内核实现 |
| 典型用途 | 资源计数、线程间同步和事件通知 | 保护共享资源和临界区 |

基本选用原则：保护共享资源优先使用互斥量；表示资源数量或发送通知时使用信号量。是否允许在中断中调用，以及应该使用哪个 ISR 安全接口，必须按目标 RTOS 的 API 约束处理。

### 6.2 常见 IPC 误用

- 忽略获取接口的返回值，在超时后仍按“已经获得资源”继续执行；
- 在线程仍等待或使用 IPC 对象时将其删除；
- 在不允许阻塞的中断上下文调用等待接口；
- 持有互斥量时执行无限期等待或阻塞发送；
- 把二值信号量当作事件计数器，导致重复事件合并；
- 消息队列发送长度与创建时的消息大小不一致；
- 错误地假设唤醒线程会立即运行，忽略调度和优先级影响。

这类错误不一定立即崩溃。所有带超时或可能失败的 IPC 调用都应检查返回值，并明确失败后资源状态是否需要回滚。

---

## 7. 竞态条件与重入

线程之间、线程与中断之间同时访问共享数据时，即使没有使用动态内存，也可能因竞态破坏状态。

```c
shared_count++;
```

这条 C 语句通常包含读取、加一和写回多个步骤。如果线程和中断同时执行，就可能丢失一次更新。`volatile` 只能约束编译器对访问的处理，不能把复合操作变成原子操作，也不能替代锁、临界区或原子指令。

常见问题包括：

- 中断修改链表时，线程同时遍历或删除节点；
- 多个线程复用同一静态缓冲区；
- 不可重入库函数被多个上下文同时调用；
- 检查条件与执行操作之间被其他线程改变状态；
- 回调执行期间对象被另一线程释放。

选择保护方式时应考虑调用上下文：线程之间可以使用互斥量，中断与线程之间通常需要短临界区、原子操作或专用的中断安全接口。不要在中断中等待会阻塞的互斥量。

---

## 8. RTOS 异常现场保存

### 8.1 CPU fault 现场

发生 HardFault、MemManage Fault、BusFault 或 UsageFault 时，应继续保留上一篇文章介绍的内容：

- MSP、PSP 和 EXC_RETURN；
- 硬件栈帧中的 R0-R3、R12、LR、PC 和 xPSR；
- M3/M4 的 CFSR、HFSR、BFAR 和 MMFAR；
- 与固件完全匹配的 ELF、MAP 和构建标识。

### 8.2 RTOS 上下文

为了判断异常与哪个线程、栈或 IPC 对象有关，还应按目标 RTOS 能力保存：

- 当前线程名称、状态、优先级和错误码；
- 线程栈起始地址、大小、保存的 SP 和栈水位；
- 当前等待的 IPC 对象和剩余超时时间；
- 互斥量持有者、持有计数和等待队列；
- 堆总量、剩余量、最低余量及最近的分配记录；
- 最近若干次线程切换和 IPC 操作记录。

运行中的线程与已经切出的线程，寄存器保存位置可能不同；解析任务栈必须匹配所用 RTOS 版本和 CPU 移植层，不能直接假设所有寄存器都采用同一种布局。

### 8.3 系统卡死现场

死锁、优先级反转和部分竞态不会触发 CPU fault。此时需要由看门狗预警阶段、监控线程或外部调试器主动采集线程列表和 IPC 状态。若直接等待最终复位，只保存 HardFault 寄存器往往得不到有效信息。

现场保存代码应避免使用已经损坏的堆、阻塞式 IPC 或复杂日志路径。保存目标也不能与待保存的 SRAM 区域重叠。
